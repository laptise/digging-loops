import type { NextPage } from "next";
import { Layout } from "../components/layout";

const Home: NextPage = () => {
  return (
    <Layout pageTitle="s" mainId="22">
      da
    </Layout>
  );
};

export default Home;
